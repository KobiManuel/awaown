// Single source of truth for the deployed site's base URL — used to build
// shareable links (partner referral links, etc.) so they point at wherever
// this is actually deployed, not a placeholder domain that was never real.
export const SITE_URL = "https://awaown-ten.vercel.app";
